/**
 * Name of the product the way its own page writes it
 */
export const PROMPTBOOK_CODER_NAME = 'Promptbook coder';

/**
 * Page of Promptbook coder, which every badge of this site leads to
 */
export const PROMPTBOOK_CODER_URL = 'https://coder.ptbk.io/';

/**
 * What the badge says about the page it sits on
 *
 * Note: It stays in English even on a Czech page, because it names a product and leads to an English page, the way the
 *       "Built with" badges of other tools do.
 */
export const PROMPTBOOK_CODER_BADGE_LABEL = `Done by ${PROMPTBOOK_CODER_NAME}`;
