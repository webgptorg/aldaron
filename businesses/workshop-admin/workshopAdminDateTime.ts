// Kept as a compatibility module for existing workshop-admin imports. The
// implementation is shared with every admin form that edits timestamps.
export { fromDateTimeLocalValue, toDateTimeLocalValue } from '@/lib/dateTimeLocal';
