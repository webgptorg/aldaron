/**
 * Shape shared by the small status badges in the header of the workshop room
 *
 * Note: Only the layout and the typography are shared. Every badge adds the colours which tell it apart.
 */
export const WORKSHOP_ROOM_BADGE_CLASS_NAME =
    'flex min-w-0 max-w-full shrink items-center gap-2 self-end rounded-full border text-xs font-medium leading-5 sm:self-auto';
