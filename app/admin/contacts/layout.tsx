import { ADMIN_CONTACTS_METADATA } from '@/lib/metadata/site-page-definitions';

export const metadata = ADMIN_CONTACTS_METADATA;

export default function AdminContactsLayout({ children }: { children: React.ReactNode }) {
    return children;
}
