import { ADMIN_WORKSHOP_METADATA } from '@/lib/metadata/site-page-definitions';

export const metadata = ADMIN_WORKSHOP_METADATA;

export default function AdminWorkshopLayout({ children }: { children: React.ReactNode }) {
    return children;
}
