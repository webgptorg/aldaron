[ ] !!!!

[✨🎐] Logic should be make on backend no in DB functions

- For example look at `migrations/2026-08-2800-community-projects.sql`, it makes problems like: `function gen_random_bytes(integer) does not exist`
- The solution is to move the logic from DB functions to backend code, and keep the DB functions only for data storage and retrieval.
- Do a migration to remove the logic from DB functions and move it to backend code.
- Keep in mind the DRY _(don't repeat yourself)_ principle.
- Do a analysis of the current functionality before you start implementing.
- Add the changes into the [changelog](./changelog/_current-preversion.md)
