[x] by OpenAI Codex `gpt-5.6-terra` thinking `max` (ChatGPT account) - Implementation ~$0.6294 35 minutes; Testing 2 minutes

[✨🏦] Create end to end tests

- Test basic things like page loads
- Especially focus on the registration forms across the app, like the registration to the newsletter at the footer, registration forms, and lead forms, and create their tests for submissions.
- During the test, you can submit the forms to the configured database, just use a test email with `@example.com` domain
- Do not delete data after the test is done.
- Instead create a garbage collector for testing data script to delete all test data from the database, and add it to the `package.json` as `npm run delete-test-data` and also to `terminals.json`
- Do not test the admin.
- Add the test script to the `package.json` as `npm run test-e2e` and also to `terminals.json`
    - In `terminals.json` make both variants headless and headed, so you can run the tests in both modes.
- Record the video of the tests running, and save it to `tests/e2e/videos/` with a timestamp in the filename.
    - gitignore these files
- Keep in mind the DRY _(don't repeat yourself)_ principle.
- Do a analysis of the current functionality before you start implementing.
- Add the changes into the [changelog](./changelog/_current-preversion.md)

