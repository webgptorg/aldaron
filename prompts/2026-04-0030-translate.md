[ ] !

[✨🔵] The homepage should be localized both in Czech and English.

- Now `/cs` is the source of the truth and `/en` is just untranslated copy of it.
- On the other hand `Footer` component is in english, there should be 2 variants `<Footer language="cs"/>` and `<Footer language="en"/>` _(the current one)_
- Keep the `/cs` vesrion as is (instead of `Footer`) and translate it to English in the `/en` version.
- When user navigates to `/` it should redirect to `/cs` or `/en` based on the browser language settings
- Keep in mind the DRY _(don't repeat yourself)_ principle and try to smartly ballance it with the fact that we want to have two separate versions of the page (one in Czech and one in English) but not want to have a lot of duplicated code between them.
- Other pages (like`/pro-mesta` or `/ai-supervize`) should be intact and only the homepage should be translated for now
    - Effectively only `/`, `/cs` and `/en` pages should be affected by this change.
- Do a proper analysis of the current functionality before you start implementing.
- Update `AGENTS.md` documentation if needed to reflect the changes you made.
